name: zadanie 2
on:
  push:
    branches: [main, 'feature/**']
    path-ignore: ['docs/**']
  pull_request:
    branches: [main]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --version

